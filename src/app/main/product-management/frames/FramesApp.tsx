import { Outlet } from 'react-router-dom'
// import withReducer from 'app/store/withReducer'
// import reducer from './store'

export default function FramesApp() {
  return <Outlet />
}
// export default withReducer('framesApp', reducer)(FramesApp)
